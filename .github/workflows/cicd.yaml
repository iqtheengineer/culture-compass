name: CI – Quality and Security Checks

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  quality-and-security:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Basic HTML sanity check (fails if index.html is missing)
      - name: Check required files
        run: |
          test -f index.html

      # Secret scanning (stable, widely used)
      - name: Secret scanning
        uses: gitleaks/gitleaks-action@v2
